PORT=4000
MONGODB_URI=mongodb://localhost:27017/videoonix
JWT_SECRET=tu-clave-secreta-muy-larga-y-segura
FRONTEND_URL=http://localhost:5173

# URL pública del backend. WishApp descarga la imagen desde esta URL; si es localhost, la generación real no funciona.
# Producción: https://api.tudominio.com
# Local con generación real: usa ngrok (ngrok http 4000) y pon aquí la URL del túnel, ej. https://xxxx.ngrok.io
PUBLIC_URL=http://localhost:4000

# Opcional: URL del backend para el frontend (evita depender del proxy de Vite).
# Si la app no conecta con la API, pon: VITE_API_URL=http://localhost:4000
# VITE_API_URL=http://localhost:4000
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
# Wallets para recibir USDT (solo testing/desarrollo; en producción usa tus propias wallets)
# BEP20: dirección falsa para testear compras sin enviar USDT real
USDT_BEP20_WALLET=0xdEaD000000000000000000000000000000000000
USDT_TRC20_WALLET=T...

# =============================================================================
# APIs externas (server/src/config/apiKeys.js)
# Centro del negocio: WishApp → generación de vídeos por IA. Ver docs/INTEGRACION-WISHAPP-VIDEOS.md
# =============================================================================

# WishApp — API central: generación de vídeos por IA (undress video). Token solo en servidor; nunca en frontend.
WISHAPP_API_TOKEN=
# WISHAPP_API_BASE_URL=https://api.wishapp.online

# Chat local (Ollama + modelo GGUF) — prioridad si está configurado
# Ver docs/OLLAMA-LOCAL-INTEGRATION.md
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=glm-uncensored

# Chat remoto: Hugging Face Router — https://router.huggingface.co/v1
# Modelo por defecto: zai-org/GLM-4.7-Flash:novita
HF_TOKEN=
# HF_CHAT_MODEL=zai-org/GLM-4.7-Flash:novita
