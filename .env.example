PORT=4000
MONGODB_URI=mongodb://localhost:27017/videoonix
JWT_SECRET=tu-clave-secreta-muy-larga-y-segura
FRONTEND_URL=http://localhost:5173

# URL pública del backend (para que WishApp pueda descargar la imagen en undress_video). En producción usa tu dominio (ej. https://api.tudominio.com).
# PUBLIC_URL=http://localhost:4000

# Opcional: URL del backend para el frontend (evita depender del proxy de Vite).
# Si la app no conecta con la API, pon: VITE_API_URL=http://localhost:4000
# VITE_API_URL=http://localhost:4000
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
# Wallets para recibir USDT (solo testing/desarrollo; en producción usa tus propias wallets)
# BEP20: dirección falsa para testear compras sin enviar USDT real
USDT_BEP20_WALLET=0xdEaD000000000000000000000000000000000000
USDT_TRC20_WALLET=T...

# =============================================================================
# APIs externas (tokens unificados en server/src/config/apiKeys.js)
# Cada token se usa solo en su API: WishApp → balance, Hugging Face → chat.
# =============================================================================

# WishApp (balance de puntos) — https://api.wishapp.online/en/docs
# Cómo obtener: inicia sesión en https://wishapp.online y obtén el Bearer token.
WISHAPP_API_TOKEN=
# Opcional: base URL (por defecto https://api.wishapp.online)
# WISHAPP_API_BASE_URL=https://api.wishapp.online

# Hugging Face (chat con Gemma) — https://huggingface.co/docs/api-inference
# Cómo obtener: https://huggingface.co/settings/tokens → Create token → tipo "Inference".
HUGGINGFACE_API_TOKEN=
# Opcional: modelo (por defecto mlabonne/gemma-3-27b-it-abliterated)
# HF_CHAT_MODEL=mlabonne/gemma-3-27b-it-abliterated
